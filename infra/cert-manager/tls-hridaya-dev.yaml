apiVersion: cert-manager.io/v1
kind: Certificate

metadata:
  name: tls-hridaya-dev
  namespace: traefik

spec:
  secretName: tls-hridaya-dev
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - 'hridaya.dev'
    - '*.hridaya.dev'
