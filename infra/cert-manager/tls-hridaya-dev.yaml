apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls-hridaya-dev
  namespace: traefik
spec:
  dnsNames:
    - hridaya.dev
    - '*.hridaya.dev'
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  secretName: tls-hridaya-dev
