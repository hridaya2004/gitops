apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: grafana-pvc
  annotations:
    argocd.argoproj.io/sync-options: Prune=false

spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
