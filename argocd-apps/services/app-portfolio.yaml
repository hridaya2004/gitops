apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: portfolio
  namespace: argocd
spec:
  destination:
    namespace: portfolio
    server: https://kubernetes.default.svc
  project: default
  source:
    path: k8s/
    repoURL: https://github.com/hridaya2004/portfolio.git
    targetRevision: main
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
