apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: portfolio
  namespace: argocd

spec:
  project: default
  source:
    repoURL: https://github.com/hridaya2004/gitops.git
    targetRevision: main
    path: apps/portfolio/env/prod
  destination:
    server: https://kubernetes.default.svc
    namespace: portfolio-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
